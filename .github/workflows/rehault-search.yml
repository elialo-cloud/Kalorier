name: Rehaul search engine

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  rehaul:
    runs-on: ubuntu-latest
    permissions:
      contents: write
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Patch app.js
        env:
          PATCH_B64: cHl0aG9uIHBhdGNoIHdpbGwgYmUgcHJvdmlkZWQgYmVsb3c=
        run: echo "Temporary patch marker"